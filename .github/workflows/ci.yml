name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-and-validate:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install beautifulsoup4 fuzzywuzzy python-Levenshtein ytmusicapi black requests

    - name: Check python style formatting
      run: |
        black --check maintenance.py maintenance_test.py

    - name: Validate scripts compile
      run: |
        python -m py_compile maintenance.py maintenance_test.py

    - name: Run unit tests
      run: |
        python -m unittest discover -p "*_test.py"

    - name: Verify YouTube links are active
      run: |
        python maintenance.py test-links
